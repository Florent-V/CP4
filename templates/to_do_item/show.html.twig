{% extends 'base.html.twig' %}

{% block title %}ToDoItem{% endblock %}

{% block body %}
    <h1>ToDoItem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ to_do_item.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ to_do_item.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_to_do_item_index') }}">back to list</a>

    <a href="{{ path('app_to_do_item_edit', {'id': to_do_item.id}) }}">edit</a>

    {{ include('to_do_item/_delete_form.html.twig') }}
{% endblock %}
