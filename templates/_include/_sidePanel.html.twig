<nav class="side-panel navbar navbar-dark navbar-expand-lg bg-dark p-lg-3 pt-lg-4 align-items-lg-start">
    <div class="container-fluid flex-lg-column">
        <img src="{{ asset('build/images/mainlogo1.png') }}" alt="">
        <a class="navbar-brand m-0" href="{{ path('app_home') }}"><h1>Splitter !</h1></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse flex-column" id="navbarNav">
            <div class="info-user my-2">
                <i class="bi bi-person-fill"></i>
                <p class="text-center m-0" >Bienvenue
                    {{ app.user.pseudo ?? "Admin" }}
                </p>
            </div>
            <div class="mt-3 d-flex justify-content-between align-items-center gap-3">
                <h2 class="m-0">Mes Splits </h2>
                <a class="btn btn-primary rounded-circle" href="{{ path('app_splitter_new') }}"><i class="bi bi-plus-lg"></i></a>
            </div>

            <ul class="navbar-nav flex-lg-column">
                {% for split in splitters %}
                    <li class="nav-item d-flex align-items-center">
                        <a class="nav-link" aria-current="page" href="{{ path('app_splitter_show', { 'id': split.id }) }}">
                            <span class="mdi mdi-format-list-bulleted">{{ split.name }}</span>
                        </a>
                        {% if split.ownedBy == app.user %}
                            <a class="link-info" href="{{ path('app_splitter_edit', {'id': split.id}) }}">
                                <i class="bi bi-pencil-square"></i>
                            </a>
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</nav>
