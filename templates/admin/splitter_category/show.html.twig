{% extends 'base.html.twig' %}

{% block title %}SplitterCategory{% endblock %}

{% block body %}

    <main class="container-xl">
        <h1>SplitterCategory</h1>

        <table class="table">
            <tbody>
            <tr>
                <th>Id</th>
                <td>{{ splitter_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ splitter_category.name }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('admin_app_splitter_category_index') }}">back to list</a>

        <a href="{{ path('admin_app_splitter_category_edit', {'id': splitter_category.id}) }}">edit</a>

        {{ include('admin/splitter_category/_delete_form.html.twig') }}

    </main>

{% endblock %}
