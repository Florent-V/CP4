<form class="deleteForm" method="post" action="{{ path('admin_app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn nav-link"><i class="bi bi-trash3-fill"></i></button>
</form>
