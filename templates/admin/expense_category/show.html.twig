{% extends 'base.html.twig' %}

{% block title %}ExpenseCategory{% endblock %}

{% block body %}
    <h1>ExpenseCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ expense_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ expense_category.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ expense_category.type }}</td>
            </tr>
            <tr>
                <th>Icon</th>
                <td>{{ expense_category.icon }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_app_expense_category_index') }}">back to list</a>

    <a href="{{ path('admin_app_expense_category_edit', {'id': expense_category.id}) }}">edit</a>

    {{ include('admin/expense_category/_delete_form.html.twig') }}
{% endblock %}
