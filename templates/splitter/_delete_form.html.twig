<form title="supprimer le splitter" method="post" action="{{ path('app_splitter_delete', {'id': splitter.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ splitter.id) }}">
    <button class="btn text-light p-0"><i class="bi bi-trash3-fill"></i></button>
</form>
